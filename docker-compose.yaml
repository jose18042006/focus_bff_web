services:
  bff_web:
    build:
      context: .
    ports:
      - "8005:8000"
    environment:
      - PORT=8000
      - SECRET_KEY=clave_super_secreta
      - ROOMS_SERVICE_URL=http://host.docker.internal:8004
      - INVENTORY_SERVICE_URL=http://host.docker.internal:8003
      - STATS_SERVICE_URL=http://host.docker.internal:8002
      - AUTH_SERVICE_URL=http://host.docker.internal:8001
    extra_hosts:
      - "host.docker.internal:host-gateway"